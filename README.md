# Angel

Consumer-facing landing page for Angel Friends, the free friends and family beta.

## GitHub Pages

This repository is ready to serve from GitHub Pages using:

- Branch: `main`
- Folder: `/`

The site is static HTML/CSS/JS and uses the local asset in `assets/campaign-calendar-signals.png`.

## Beta Registration

The current form is a static front-end beta-interest interaction. Before sending public traffic, connect the form to a real lead-capture destination such as a backend endpoint, Google Form, Tally, Typeform, Buttondown, ConvertKit, Airtable, or another CRM/list provider.

